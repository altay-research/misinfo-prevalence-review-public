# GPT cross-check package (corpus repair, 2026-09)

155 estimate rows from 78 studies, in 4 batches of up to 40 (order shuffled, seed 20260903).
Each batch is TWO files with the same rows: `verify_NN.csv` (our extraction shown, verdicts) and `blind_NN.csv` (construct coded blind).
Paper texts in `texts/`. One batch per fresh session; see INSTRUCTIONS.md.
Our codes for the blind task are NOT in this folder.
